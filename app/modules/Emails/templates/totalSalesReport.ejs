<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; background-color: #f5f5f7; color: #1d1d1f; margin: 0; padding: 20px; }
        .container { max-width: 600px; margin: auto; background-color: #ffffff; padding: 30px; border-radius: 12px; border: 1px solid #d2d2d7; }
        .header { text-align: center; border-bottom: 1px solid #e5e5e6; padding-bottom: 20px; margin-bottom: 20px; }
        h1 { font-size: 26px; color: #1d1d1f; margin: 0 0 5px 0; }
        h2 { font-size: 18px; color: #1d1d1f; margin-top: 30px; margin-bottom: 15px; border-bottom: 2px solid #e5e5e6; padding-bottom: 5px; }
        .kpi-grid { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 15px; text-align: center; margin-bottom: 20px; }
        .kpi-card { background-color: #f5f5f7; padding: 15px; border-radius: 8px; }
        .kpi-card .value { font-size: 20px; font-weight: 600; color: #0071e3; margin: 0 0 5px 0; }
        .kpi-card .label { font-size: 12px; color: #6e6e73; margin: 0; }
        .data-table { width: 100%; border-collapse: collapse; font-size: 14px; }
        .data-table th, .data-table td { text-align: left; padding: 10px; border-bottom: 1px solid #e5e5e6; }
        .data-table th { color: #6e6e73; font-weight: normal; }
        .data-table .align-right { text-align: right; }
        .footer { text-align: center; margin-top: 30px; font-size: 12px; color: #86868b; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header"><h1>Daily Sales Summary</h1><p>Branch: <strong><%= branchName %></strong> | <%= reportDate %></p></div>
        <h2>Performance Overview</h2>
        <div class="kpi-grid">
            <div class="kpi-card"><p class="value"><%= kpis.totalRevenue.toFixed(2) %> Taka</p><p class="label">Total Revenue</p></div>
            <div class="kpi-card"><p class="value"><%= kpis.totalOrders %></p><p class="label">Total Orders</p></div>
            <div class="kpi-card"><p class="value"><%= kpis.avgOrderValue.toFixed(2) %> Taka</p><p class="label">Avg. Order Value</p></div>
        </div>
        <% if (salesByOrderType && salesByOrderType.length > 0) { %>
            <h2>Sales by Order Type</h2>
            <table class="data-table"><% salesByOrderType.forEach(type => { %><tr><th><%= type.name %></th><td class="align-right"><%= type.revenue.toFixed(2) %> Taka (<%= type.orders %> orders)</td></tr><% }); %></table>
        <% } %>
        <% if (topSellingItems && topSellingItems.length > 0) { %>
            <h2>Top 5 Selling Items</h2>
            <table class="data-table"><% topSellingItems.forEach(item => { %><tr><td><%= item.name %></td><td class="align-right"><%= item.quantity %> sold</td><td class="align-right"><%= item.revenue.toFixed(2) %> Taka</td></tr><% }); %></table>
        <% } %>
        <% if (paymentSummary && paymentSummary.length > 0) { %>
            <h2>Payment Method Summary</h2>
            <table class="data-table"><% paymentSummary.forEach(method => { %><tr><th><%= method.name %></th><td class="align-right"><%= method.amount.toFixed(2) %> Taka</td></tr><% }); %></table>
        <% } %>
        <div class="footer"><p>This is an automated report from Leave POS.</p></div>
    </div>
</body>
</html>